pipeline {
    agent any

    tools {
        maven 'M3'  // Assurez-vous que Maven est installé et que le nom est correct
    }

    environment {
        // Ces variables ne sont plus nécessaires si vous ne utilisez pas PostgreSQL
        // POSTGRES_HOST = "localhost"
        // POSTGRES_PORT = "5432"
    }

    stages {
        stage('Clone Repository') {
            steps {
                script {
                    def repoDir = 'spring-petclinic'
                    if (!fileExists(repoDir)) {
                        bat "git clone https://github.com/Fzsalhi2002/spring-petclinic.git"
                    } else {
                        echo "Le dépôt existe déjà dans le répertoire ${repoDir}, passage à l'étape suivante."
                    }
                }
            }
        }

        stage('Build') {
            steps {
                dir('spring-petclinic') {
                    bat 'mvnw.cmd clean install'
                }
            }
        }

        stage('Test') {
            steps {
                dir('spring-petclinic') {
                    // Tester avec un profil sans PostgreSQL
                    bat 'mvnw.cmd test -Dspring.profiles.active=test' // Utiliser un profil de test ou le profil par défaut
                }
            }
        }

        stage('Package') {
            steps {
                dir('spring-petclinic') {
                    bat 'mvnw.cmd package'
                }
            }
        }
    }

    post {
        success {
            echo "Build and tests succeeded!"
        }
        failure {
            echo "Build failed!"
        }
    }
}
